<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- AudioBridgeService: raw PCM from the guest over 192.168.117.1:4713 to
         AudioTrack. FGS mediaPlayback is required on Android 14+ for a
         long-lived audio session; POST_NOTIFICATIONS lets the FGS banner show
         on 13+ without a user prompt (channel is IMPORTANCE_MIN so it's a
         status-line dot only). RECEIVE_BOOT_COMPLETED starts the service on
         boot so guest audio works without a per-boot tap. INTERNET is for
         the loopback-only ServerSocket. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- connectedDevice is the boot-time fallback: Android 15+ forbids starting
         a mediaPlayback FGS from BOOT_COMPLETED, but connectedDevice is allowed. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.Determination">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity
            android:name=".DesktopShortcutActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:noHistory="true"
            android:theme="@style/Theme.Determination.Dialog" />

        <!-- Brief, non-interactive cover for the SurfaceFlinger handoff. The
             root toggle scripts launch it explicitly; it never owns the mode
             switch and is deliberately not exported to third-party apps. -->
        <activity
            android:name=".ModeTransitionActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:noHistory="true"
            android:screenOrientation="unspecified"
            android:theme="@style/Theme.Determination.Transition" />

        <activity
            android:name=".ShareToLinuxActivity"
            android:exported="true"
            android:label="@string/share_to_linux"
            android:noHistory="true"
            android:theme="@style/Theme.Determination.Dialog">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>

        <!-- Quick Settings tile: one tap to enter/leave desktop mode. -->
        <service
            android:name=".DesktopTileService"
            android:exported="true"
            android:icon="@drawable/ic_desktop"
            android:label="@string/tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Guest audio bridge: keeps a ServerSocket on 192.168.117.1:4713
             and pipes incoming PCM to AudioTrack (runs in this app's SELinux
             context, which is what the framework audio service accepts). -->
        <service
            android:name=".AudioBridgeService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback|connectedDevice" />

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
