# DecemberOS phoc config — installed to guest /etc/phoc.ini; desktop-on
# passes -C /etc/phoc.ini.
# The panel is 1080x2340 @ ~403dpi; phoc's default scale of 1 renders
# unreadably tiny text (melissa, 2026-07-06). Scale 3 = 360x780 logical,
# the usual choice for this panel class.
[output:HWCOMPOSER-1]
scale = 3
